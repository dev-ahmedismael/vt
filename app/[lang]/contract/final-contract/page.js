import FinalContract from "@/components/contract/FinalContract";
import React from "react";

const page = () => {
  return (
    <main>
      <FinalContract />
    </main>
  );
};

export default page;
